const isPalindrome = require('../1-palindrome');

string1 = 'emadame'
string2 = 'notpalindrome'

console.log(`${string1} is ${isPalindrome(string1) ? 'a palindrome' : 'not a palindrome'}`);
console.log(`${string2} is ${isPalindrome(string2) ? 'a palindrome' : 'not a palindrome'}`);