const isPrime = require('../12-is_prime');

n = 83;
n2 = 80
console.log(`${n} is ${isPrime(n) ? 'a prime number' : 'not a prime number' }`);
console.log(`${n2} is ${isPrime(n2) ? 'a prime number' : 'not a prime number' }`);