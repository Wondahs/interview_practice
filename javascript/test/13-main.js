const largestInNest = require('../13-largest_in_nest');

const nestArr = [[3, 4, 58], [709, 8, 9, [1000, 11]], [111, 2]]

console.log('Nested array:', nestArr);
console.log('Largest in nested array:', largestInNest(nestArr));
